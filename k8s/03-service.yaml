apiVersion: v1
kind: Service
metadata:
  name: kavita-service
  namespace: biblioteca
spec:
  type: LoadBalancer 
  selector:
    app: kavita
  ports:
    - name: http-web
      protocol: TCP
      port: 8888
      targetPort: 5000
